# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
.venv/
env/
.env
.env.local
.env.*.local
*.egg-info/
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# Node / Next.js (root-level fallbacks — dashboard/ has its own)
node_modules/
.next/
out/
build/
dist/
*.tsbuildinfo
next-env.d.ts

# Logs
*.log
logs/
docker-compose.yml.bak
# Secrets / keys (NEVER commit)
*.pem
*.key
secrets/
.aws/
.gcp/
.gcp-credentials.json
cloudflare_frontend.log
cloudflare_backend.log
# AMD-specific
*.safetensors
*.bin
checkpoints/
runs/
wandb/

# Test artifacts
test.html
test_cache/
.cookies.txt
test_*-passed/

# Misc
*.tmp
*.bak
.cache/

# Superpowers brainstorming mockups (local only)
.superpowers/

# ChromaDB persistent vector store (regenerated from scraper pipeline)
chroma_db/
.agents/
.skill-archive/
.worktrees/
_archive_stale/

# Chroma vector DB (local, not committed)
.chroma_db/
backend/.chroma_db/

# Factory output (deployed token CA — local, regenerated by scripts/factory.js)
backend/factory-latest.json
factory-latest.json

# Submission / local notes (not part of source)
SUBMISSION_TEXT.md

# Local planning / scratch .md docs (NEVER push to repo — local only)
# Ignore ALL root-level .md except the official repo docs below.
/*.md
!README.md
!SUBMISSION.md
# Internal dev-log / on-chain proof history — local only, NEVER push (stealth)
docs/session-25-delivery-track.md
AaaS_PLAN.md
*_PLAN.md
PLAN_*.md
MEMORY_A2Z*.md

# Secrets / sensitive patterns — defense in depth (never commit)
*.env
*.env.*
!.env.example
backend/.env
.env
*.pem
*.key
secrets/
# Private key / seed / mnemonic literals (heuristic)
*PRIVATE_KEY*
*VAULT_PRIVATE*
*secret*
*SECRET*
